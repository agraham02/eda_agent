[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    smoke: Essential smoke tests for demos
    integration: End-to-end workflow tests
addopts = 
    --verbose
    --tb=short
    --strict-markers
